# TodoTree 待办

- [x] 完成共享契约的运行验收
	- [x] 刷新页面后确认浏览器不再加载 `better-sqlite3`、`node:fs` 等 Node 专用依赖。
	- [x] 确认状态、执行者和渲染模板均由 `contract.ts` 的验证器生产类型与共享映射。
	- [x] 确认 Hono、MCP JSON Schema 和页面消费同一映射，没有重复数字表或接口传递层。
- [ ] 完成 MCP 新会话契约验收
	- [ ] 新会话重新连接后确认 `node.add`、`node.set` 的 `status` 明确显示全部数字含义。
	- [x] 当前成品契约确认 `agent` 只允许 `1 parent`，已移除工作队状态与执行者的旧契约。
	- [ ] 恢复使用当前 `conversation.init`、`project.tree` 和节点接口维护真实项目树。
- [x] 完成 Tree 页面交互验收
	- [x] 确认隐藏数据根节点和无意义总数标题，首页直接显示各具体项目。
	- [x] 确认节点默认显示状态，悬停后在左侧显示 ID 与编辑图标。
	- [x] 当前约 300 个节点只渲染 15 个展开路径 Tree 行，折叠分支不生成整棵可见 Tree。
- [x] 完成抽屉渲染验收
	- [x] 确认 `markdown` 节点在抽屉完整渲染 Markdown，Tree 中保持单行摘要。
	- [x] 确认 `typescript`、`text`、`file`、`project` 分别按共享模板语义显示。
	- [x] 确认状态顺序为待确认、待办、运行中、阻塞、已取消、已完成。
- [x] 完成最近 SSE 节点验收
	- [x] 确认右侧只保留最近 10 个不同节点 ID，新消息以绿色标记未读。
	- [x] 点击 ID 后标记已读、展开完整祖先路径并打开对应节点抽屉。
	- [x] 确认增、改、删和交流初始化均通过正向 SSE 更新页面，不增加轮询。
- [x] 清理页面运行警告
	- [x] 消除 React Router 的 `HydrateFallback` 警告。
	- [x] 实际打开 Drawer 后确认浏览器控制台没有弃用警告。
- [x] 同步 TodoTree 项目书
	- [x] 新增 `src/todotree/contract.ts` 的公开成员及直接消费链。
	- [x] 删除已移除的前端 `src/todotree/status.ts` 节点。
	- [x] 将最终源码、MCP 契约、页面表现和项目树逐项对齐。
- [x] 完成人类可见验收
	- [x] 使用协同浏览器打开 TodoTree，让方先生能直接看到完整验收过程。
	- [x] 首页直接显示具体项目，不显示数据根节点和无意义总数标题。
	- [x] 悬停真实节点时，状态切换为左侧 ID 与编辑图标。
	- [x] 分别打开 Markdown、TypeScript、普通文本、文件和项目节点，确认 Tree 紧凑、抽屉完整。
	- [x] 保持页面不刷新，通过 Hono/MCP 修改真实节点，确认右侧立即出现绿色未读 ID。
	- [x] 点击最近 ID 后确认标记已读、展开完整祖先路径并打开对应抽屉。
	- [x] 现场执行增、改、删，确认全部由 SSE 实时更新且没有轮询。
	- [x] 展示 MCP 实际输入契约，确认数字含义与页面状态文字完全一致。
	- [x] 检查浏览器控制台无 Node 模块加载错误和业务异常。
	- [x] 清理临时验收节点，将页面停留在便于方先生继续体验的状态。
	- [x] 类型检查、依赖边界、编码和 diff 检查只作为页面验收的底层证据。
